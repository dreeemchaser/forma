spring:
  application:
    name: api
  datasource:
    password: ${DB_PASSWORD}

ai:
  provider: claude
  api-key: ${ANTHROPIC_API_KEY}

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html